# 🏗️ SƠ ĐỒ KIẾN TRÚC VÀ LUỒNG HOẠT ĐỘNG

## 📐 KIẾN TRÚC TỔNG QUAN

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT (Frontend/Mobile)                  │
└───────────────────────┬─────────────────────────────────────┘
                        │ HTTP Request
                        │ (JWT Token in Header)
                        ↓
┌─────────────────────────────────────────────────────────────┐
│              SPRING SECURITY FILTER CHAIN                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         SecurityConfig                                │  │
│  │  - Public endpoints: /api/auth/**, /swagger-ui/**    │  │
│  │  - Protected endpoints: All others                    │  │
│  └───────────────────┬──────────────────────────────────┘  │
│                       ↓                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │      JwtAuthenticationFilter                          │  │
│  │  1. Extract JWT token                                 │  │
│  │  2. Validate token (JwtUtil)                          │  │
│  │  3. Decode → userId, role                             │  │
│  │  4. Load permissions (PermissionService)             │  │
│  │  5. Match URL + METHOD                                │  │
│  │  6. Set Authentication Context                        │  │
│  └───────────────────┬──────────────────────────────────┘  │
└───────────────────────┼─────────────────────────────────────┘
                        │
                        ↓ (If authenticated & authorized)
┌─────────────────────────────────────────────────────────────┐
│                    CONTROLLER LAYER                          │
│  ┌──────────────────┐         ┌──────────────────┐         │
│  │ BookController   │         │ AuthController   │         │
│  │ - CRUD Books     │         │ - Login          │         │
│  └────────┬─────────┘         └────────┬─────────┘         │
└───────────┼────────────────────────────┼───────────────────┘
            │                            │
            ↓                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     SERVICE LAYER                            │
│  ┌──────────────────┐         ┌──────────────────┐         │
│  │ BookService      │         │ PermissionService │         │
│  │ (Interface)      │         │ - Load permissions│         │
│  └────────┬─────────┘         └────────┬─────────┘         │
│           │                            │                    │
│           ↓                            │                    │
│  ┌──────────────────┐                 │                    │
│  │ BookServiceImpl  │                 │                    │
│  │ - Business Logic │                 │                    │
│  └────────┬─────────┘                 │                    │
└───────────┼───────────────────────────┼────────────────────┘
            │                            │
            ↓                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    REPOSITORY LAYER                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │BookRepo  │  │Category  │  │RoleRepo  │  │UserRepo  │  │
│  │          │  │Repo      │  │          │  │          │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
└───────┼──────────────┼──────────────┼──────────────┼────────┘
        │              │              │              │
        └──────────────┴──────────────┴──────────────┘
                        ↓
        ┌───────────────────────────────┐
        │      DATABASE (MySQL)          │
        │  - books                       │
        │  - categories                  │
        │  - users                       │
        │  - roles                       │
        │  - permissions                 │
        │  - user_roles                  │
        │  - role_permissions            │
        └───────────────────────────────┘
```

---

## 🔄 LUỒNG XỬ LÝ REQUEST (Chi tiết)

### **Luồng 1: Protected Endpoint (Ví dụ: POST /api/books)**

```
┌─────────┐
│ Client  │
└────┬────┘
     │ POST /api/books
     │ Authorization: Bearer <token>
     │ Body: { "isbn": "123", ... }
     ↓
┌─────────────────────────────────────┐
│ SecurityConfig                      │
│ - Check: Is /api/books public?      │
│ - NO → Require authentication       │
└────┬────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ JwtAuthenticationFilter             │
│                                     │
│ 1. Extract Token                    │
│    Header: "Authorization: Bearer..." │
│    → Extract: "<token>"             │
│                                     │
│ 2. Validate Token                   │
│    JwtUtil.validateToken(token)     │
│    → Check signature, expiration    │
│    → OK/FAIL                        │
│                                     │
│ 3. Decode Token                     │
│    JwtUtil.getUserIdFromToken()     │
│    → userId = 1                     │
│    JwtUtil.getRoleFromToken()       │
│    → role = "ADMIN"                 │
│                                     │
│ 4. Load Permissions                 │
│    PermissionService.getPermissions  │
│    ByRole("ADMIN")                  │
│    → Query DB: RoleRepository       │
│    → Returns: [BOOK_CREATE,          │
│                BOOK_READ,            │
│                BOOK_UPDATE,          │
│                BOOK_DELETE]          │
│                                     │
│ 5. Check Permission                 │
│    PermissionService.hasPermission(  │
│      "ADMIN",                        │
│      "/api/books",                   │
│      "POST"                          │
│    )                                 │
│    → Match với BOOK_CREATE          │
│    → URL: /api/books matches         │
│    → METHOD: POST matches            │
│    → TRUE                            │
│                                     │
│ 6. Set Authentication               │
│    SecurityContextHolder.set         │
│    Authentication(userId, role)      │
└────┬────────────────────────────────┘
     ↓ (Continue to Controller)
┌─────────────────────────────────────┐
│ BookController.createBook()         │
│                                     │
│ 1. Validate Request                 │
│    @Valid CreateBookRequest         │
│    → Check @NotBlank, @Size, etc.  │
│                                     │
│ 2. Call Service                     │
│    bookService.createBook(request)  │
└────┬────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ BookServiceImpl.createBook()        │
│                                     │
│ 1. Business Validation              │
│    - Check ISBN duplicate           │
│      BookRepository.existsByIsbn     │
│      AndStatus(isbn, ACTIVE)        │
│    - Load Category                  │
│      CategoryRepository.findById    │
│                                     │
│ 2. Convert DTO → Entity             │
│    BookMapper.toEntity(request,     │
│                      category)      │
│    → Create Book object             │
│                                     │
│ 3. Set Defaults                     │
│    book.setStatus(ACTIVE)           │
│                                     │
│ 4. Save to Database                 │
│    BookRepository.save(book)        │
│    → INSERT INTO books ...          │
│                                     │
│ 5. Convert Entity → DTO             │
│    BookMapper.toResponse(savedBook) │
│    → Create BookResponse            │
│                                     │
│ 6. Return Response                  │
│    return BookResponse              │
└────┬────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ BookController                      │
│                                     │
│ 1. Build Location Header            │
│    URI location = /api/books/1       │
│                                     │
│ 2. Return Response                  │
│    ResponseEntity.created(location)  │
│    .body(response)                  │
│    → Status: 201 Created            │
│    → Header: Location: /api/books/1  │
│    → Body: BookResponse             │
└────┬────────────────────────────────┘
     ↓
┌─────────┐
│ Client  │ ← Receives 201 Created + BookResponse
└─────────┘
```

### **Luồng 2: Login (Public Endpoint)**

```
┌─────────┐
│ Client  │
└────┬────┘
     │ POST /api/auth/login
     │ Body: { "username": "admin", "password": "admin123" }
     ↓
┌─────────────────────────────────────┐
│ SecurityConfig                      │
│ - Check: Is /api/auth/** public?    │
│ - YES → Skip JWT Filter            │
└────┬────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ AuthController.login()              │
│                                     │
│ 1. Validate Request                 │
│    @Valid LoginRequest              │
│                                     │
│ 2. Find User                        │
│    UserRepository.findByUsername    │
│    ("admin")                        │
│    → Returns: User entity           │
│                                     │
│ 3. Verify Password                  │
│    PasswordEncoder.matches(         │
│      "admin123",                    │
│      user.getPassword()             │
│    )                                │
│    → Compare plain vs hashed        │
│    → TRUE/FALSE                     │
│                                     │
│ 4. Get Role                         │
│    user.getRoles().stream()         │
│    .findFirst()                     │
│    → role = "ADMIN"                 │
│                                     │
│ 5. Generate JWT Token               │
│    JwtUtil.generateToken(           │
│      userId=1,                      │
│      role="ADMIN"                   │
│    )                                │
│    → Create JWT with payload:       │
│      { userId: 1, role: "ADMIN" }   │
│    → Sign with secret key           │
│    → Return token string            │
│                                     │
│ 6. Build Response                   │
│    JwtTokenDto.builder()            │
│      .token(token)                  │
│      .userId(1)                     │
│      .role("ADMIN")                 │
│      .build()                       │
│                                     │
│ 7. Return Response                  │
│    ApiResponse.success(             │
│      "Đăng nhập thành công",        │
│      tokenDto                       │
│    )                                │
└────┬────────────────────────────────┘
     ↓
┌─────────┐
│ Client  │ ← Receives JWT Token
└─────────┘
```

### **Luồng 3: Exception Handling**

```
┌─────────────────────────────────────┐
│ Any Layer throws Exception           │
│ Example: BookServiceImpl            │
│   throws ResourceNotFoundException   │
└────┬────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ GlobalExceptionHandler              │
│                                     │
│ 1. Catch Exception                  │
│    @ExceptionHandler(               │
│      ResourceNotFoundException      │
│    )                                │
│                                     │
│ 2. Extract Error Info               │
│    ex.getErrorCode()                │
│    → "BOOK_NOT_FOUND"               │
│    ex.getMessage()                  │
│    → "Book with ID 1 not found"     │
│                                     │
│ 3. Build Error Response             │
│    ErrorResponse(                   │
│      errorCode: "BOOK_NOT_FOUND",   │
│      message: "Book with ID..."     │
│    )                                │
│                                     │
│ 4. Map to HTTP Status               │
│    ResourceNotFoundException        │
│    → 404 Not Found                  │
│                                     │
│ 5. Return Response                  │
│    ResponseEntity.status(404)        │
│    .body(errorResponse)             │
└────┬────────────────────────────────┘
     ↓
┌─────────┐
│ Client  │ ← Receives 404 + ErrorResponse
└─────────┘
```

---

## 🔐 SECURITY FLOW (Chi tiết)

```
┌─────────────────────────────────────────────────────────────┐
│                    REQUEST ARRIVES                           │
└───────────────────────┬─────────────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │  SecurityConfig                │
        │  - Is endpoint public?         │
        └───────┬─────────────────────────┘
                │
        ┌───────┴────────┐
        │                │
        ↓ YES            ↓ NO
┌───────────────┐  ┌──────────────────────┐
│ Skip Filter   │  │ JwtAuthentication    │
│ Continue      │  │ Filter               │
└───────┬───────┘  └──────┬───────────────┘
        │                  │
        │                  ↓
        │      ┌──────────────────────────┐
        │      │ Extract Token            │
        │      │ Header: Authorization     │
        │      └──────┬───────────────────┘
        │             ↓
        │      ┌──────────────────────────┐
        │      │ Validate Token           │
        │      │ JwtUtil.validateToken()   │
        │      └──────┬───────────────────┘
        │             │
        │      ┌──────┴────────┐
        │      │               │
        │      ↓ INVALID       ↓ VALID
        │  ┌─────────┐    ┌──────────────────┐
        │  │ 403     │    │ Decode Token     │
        │  │ Forbidden│   │ - userId         │
        │  └─────────┘    │ - role           │
        │                 └──────┬───────────┘
        │                        ↓
        │                 ┌──────────────────┐
        │                 │ Load Permissions │
        │                 │ PermissionService│
        │                 │ .getPermissions  │
        │                 │ ByRole(role)     │
        │                 └──────┬───────────┘
        │                        ↓
        │                 ┌──────────────────┐
        │                 │ Check Permission │
        │                 │ Match URL+METHOD │
        │                 └──────┬───────────┘
        │                        │
        │                 ┌──────┴────────┐
        │                 │               │
        │                 ↓ NO            ↓ YES
        │            ┌─────────┐    ┌──────────────┐
        │            │ 403     │    │ Set Auth     │
        │            │ Forbidden│   │ Context      │
        │            └─────────┘    └──────┬───────┘
        │                                  │
        └──────────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │  Continue to Controller        │
        └───────────────────────────────┘
```

---

## 📊 DATA FLOW

```
┌──────────────┐
│   Request    │
│   (JSON)     │
└──────┬───────┘
       ↓
┌──────────────────┐
│  Request DTO     │
│  CreateBookRequest│
└──────┬───────────┘
       ↓
┌──────────────────┐
│  BookMapper      │
│  .toEntity()     │
└──────┬───────────┘
       ↓
┌──────────────────┐
│  Book Entity     │
│  (JPA)           │
└──────┬───────────┘
       ↓
┌──────────────────┐
│  BookRepository  │
│  .save()         │
└──────┬───────────┘
       ↓
┌──────────────────┐
│  Database        │
│  (MySQL)         │
└──────┬───────────┘
       ↓
┌──────────────────┐
│  Book Entity     │
│  (saved)         │
└──────┬───────────┘
       ↓
┌──────────────────┐
│  BookMapper      │
│  .toResponse()   │
└──────┬───────────┘
       ↓
┌──────────────────┐
│  Response DTO    │
│  BookResponse    │
└──────┬───────────┘
       ↓
┌──────────────┐
│   Response   │
│   (JSON)     │
└──────────────┘
```

---

## 🗄️ DATABASE RELATIONSHIPS

```
┌──────────┐         ┌──────────┐
│   User   │─────────│   Role   │
│          │ M    M  │          │
└──────────┘         └────┬─────┘
                          │
                          │ M
                          │
                          ↓
                    ┌──────────┐
                    │Permission│
                    │          │
                    └────┬─────┘
                         │
                         │ M
                         │
                         ↓
                    ┌──────────┐
                    │   Book   │
                    │          │
                    └────┬─────┘
                         │
                         │ M
                         │
                         ↓
                    ┌──────────┐
                    │ Category │
                    │          │
                    └──────────┘

Tables:
- users ←→ user_roles ←→ roles
- roles ←→ role_permissions ←→ permissions
- books → categories (ManyToOne)
```

---

## 🎯 TÓM TẮT

1. **Request** → SecurityConfig → JwtFilter → Controller
2. **Controller** → Service → Repository → Database
3. **Response** ← Mapper ← Entity ← Database
4. **Exception** → GlobalExceptionHandler → ErrorResponse
5. **Security**: JWT Token → Validate → Load Permissions → Check → Allow/Deny



